---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: simpleplans.upcloud.visdmin.com
spec:
  group: upcloud.visdmin.com
  names:
    kind: SimplePlan
    listKind: SimplePlanList
    plural: simpleplans
    shortNames:
    - plan
    singular: simpleplan
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Server plan status
      jsonPath: .status.status
      name: Status
      type: string
    - description: Server plan CPU core number
      jsonPath: .spec.account_ref
      name: Account
      type: string
    - description: Server plan CPU core number
      jsonPath: .spec.plan_name
      name: PlanName
      type: string
    - description: Server plan CPU core number
      jsonPath: .status.core_number
      name: CoreNumber
      type: string
    - description: Server plan memory amount
      jsonPath: .status.memory_amount
      name: MemoryAmount
      type: string
    - description: Server plan public traffic out
      jsonPath: .status.public_traffic_out
      name: PublicTrafficOut
      type: string
    - description: Server plan storage size
      jsonPath: .status.storage_size
      name: StorageSize
      type: string
    - description: Server plan storage tier
      jsonPath: .status.storage_tier
      name: StorageTier
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SimplePlan is the Schema for the simpleplans API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SimplePlanSpec defines the desired state of SimplePlan
            properties:
              account_ref:
                description: ObjectReference contains enough information to let you
                  inspect or modify the referred object.
                properties:
                  apiVersion:
                    description: API version of the referent.
                    type: string
                  fieldPath:
                    description: |-
                      If referring to a piece of an object instead of an entire object, this string
                      should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                      For example, if the object reference is to a container within a pod, this would take on a value like:
                      "spec.containers{name}" (where "name" refers to the name of the container that triggered
                      the event) or if no container name is specified "spec.containers[2]" (container with
                      index 2 in this pod). This syntax is chosen only to have some well-defined way of
                      referencing a part of an object.
                    type: string
                  kind:
                    description: |-
                      Kind of the referent.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                    type: string
                  name:
                    description: |-
                      Name of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                  namespace:
                    description: |-
                      Namespace of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                    type: string
                  resourceVersion:
                    description: |-
                      Specific resourceVersion to which this reference is made, if any.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                    type: string
                  uid:
                    description: |-
                      UID of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              plan_name:
                type: string
            type: object
          status:
            description: SimplePlanStatus defines the observed state of SimplePlan
            properties:
              core_number:
                type: integer
              memory_amount:
                type: integer
              public_traffic_out:
                type: integer
              status:
                type: string
              storage_size:
                type: integer
              storage_tier:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
